# Demo dataset for a 20 node public transport demand

Run `generate_scenario.py` to get generated scenario for a 20 node Helsinki Public Transport system with generated demand and 15-minute schedule.

Files are shaped to be ran in MATSim simulator. `config.xml` contains everything necessary to run the simulated scenario.